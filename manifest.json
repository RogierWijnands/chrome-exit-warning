{
  "name": "Chrome Exit Warning",
  "version": "1.0",
  "description": "Trigger alert before closing Chrome (to prevent accidentally closing browser).",
  "manifest_version": 2,
  "permissions": [
    "tabs"
  ],
  "background": {
    "scripts": ["main.js"],
    "persistent": false
  }
}
